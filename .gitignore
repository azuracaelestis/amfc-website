# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local env
.env
.env.*
!.env.example

# PHP
vendor/
composer.lock
*.log

# Generated static export (regenerate via tools/export-static.php)
dist/

# Raw asset drop folder — a holding area for source files as they're supplied, not part of
# the three-zone repo structure (see CLAUDE.md). Files get copied into public/assets/ once
# wired into a partial; this folder stays local and untracked.
/asset/
